# added by envy. please do not edit the lines below
LOGIN_D=$HOME/.nv/login.d
if [ -d $LOGIN_D ]; then
    for x in $LOGIN_D/* ; do
        test -f "$x" || continue
        test -x "$x" || continue
        . "$x"
    done
fi
# added by envy. please do not edit the lines above
